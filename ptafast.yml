name: ptafast
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - matplotlib
  - healpy
  - astropy
  - ipykernel
  - tqdm
  - multiprocess
  - pip
  - pip:
      - py3nj
      - ptafast
      
# System-level dependencies (install via Homebrew):
#   brew install libomp gfortran
