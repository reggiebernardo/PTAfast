name: ptafast
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - scipy
  - matplotlib
  - healpy
  - astropy
  - ipykernel
  - tqdm
  - multiprocess
  - pip
  - pip:
      - py3nj
      
# System-level dependencies (install via Homebrew):
#   brew install libomp gfortran
